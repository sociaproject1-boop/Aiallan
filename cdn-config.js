/*
 * AIALAN CDN CONFIGURATION
 *
 * Leave this empty until the Cloudflare Worker/custom domain is deployed.
 * Then set the base to your CDN hostname, for example:
 *   window.AIALLAN_CDN_BASE = 'https://cdn.aiallan.shop';
 *
 * Empty = automatic fallback to the original Supabase Storage URL.
 */
window.AIALLAN_CDN_BASE = '';
